1. The bug was that num1 and num2 are strings rather than numbers, so the result has a type of string instead of outputting a number.
2. The way to fix it is to convert num1 and num2 to numbers using Number(). Screenshot provided.
